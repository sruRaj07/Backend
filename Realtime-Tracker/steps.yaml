checks if the browser supports geo-location;
Set options for high accuracy, a 5-seconds timeout, and no caching;
Use watchPosition to track the users location automatically;
Emit te latitude and longitude via a socket with "send-location". log any errors to the console;
Initialize a map centeed at coordinates (0,0) with a zoom level of 15 using leafeffect . Add openStreet tiles to the map
Create an empty object markers;
when receiving location data via the socket, extract id, latitude, longitude, and center the map on
the new cordinates;
